import SwiftUI

#warning("Unused")
struct SettingsView: View {
    var body: some View {
        Form {
            
        }
        .formStyle(.grouped)
        .buttonStyle(.plain)
        .scrollIndicators(.never)
    }
}

#Preview {
    SettingsView()
}
