struct SheetView: View {
    // Replace: "@Environment(\.dismiss) var dismiss"
    // by:
    @Environment (\.presentationMode) var presentationMode
    
    var body: some View {
        Button("Dismiss the sheet") {
            // Replace: "dismiss()"
            // by:
            presentationMode.wrappedValue.dismiss()
        }
    }
}
