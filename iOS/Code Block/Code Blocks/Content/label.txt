// Text & SF Symbol
Label("User", systemImage: "person.fill")

// Text & Image from assets catalogue
Label("User", image: "myImage")

// Text & Custom view
Label {
    Text("User")
} icon: {
    // Custom view
    Circle()
        .fill(.secondary)
}

// Label Style
Label("User", systemImage: "person.fill")
    .labelStyle(.iconOnly)
