// iOS 26 required

@State private var attributedText: AttributedString
@State private var selection: AttributedTextSelection? = nil

var body: some View {
    TextEditor(text: $attributedText, selection: $selection)
}
