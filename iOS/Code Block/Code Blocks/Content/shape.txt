Rectangle()
    .fill(.blue)
    .stroke(.blue, lineWidth: 1)
    .frame(width: 80, height: 40)
    .padding()

Rectangle()

RoundedRectangle(cornerRadius: 6)

Circle()

Ellipse()

Capsule()

// ConventricRectangle() will match
// rounding of the container
VStack {
    ConventricRectangle()
        .fill(.indigo)
}
.padding()
.background(.gray)
.containerShape(.rect(cornerRadius: 16))
