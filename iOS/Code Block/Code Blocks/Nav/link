let link = "https://bisquit.host/en"

Link("Title", destination: URL(string: link)!)
    .font(.title2)
    .fontWeight(.bold)
    .foregroundColor(.orange)

// Custom label
Link(destination: URL(string: link)!) {
    ...
}

// Safe unwrapping
if let url = URL(string: link) {
    Link(destination: url) {
        ...
    }
}
