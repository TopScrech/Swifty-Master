NavigationLink(destination: DestinationView()) {
    // How the navigationLink looks
}

// Navigation destination as a value
List {
    NavigationLink(value: /* Data to pass*/) {
        // label
    }
}
.navigationDestination(for: /* Value type */.self) { data in
    ViewOfDestination(data: data)
}
