NavigationLink(destination: DestinationView()) {
    // label
}

// Nav destination as a value
List {
    NavigationLink(value: /* Data to pass*/) {
        // label
    }
}
.navigationDestination(for: /* Value type */.self) {
    ViewOfDestination(data: $0)
}
