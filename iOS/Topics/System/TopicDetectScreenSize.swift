import SwiftUI

struct TopicDetectScreenSize: View {
    var body: some View {
        VStack(spacing: 25) {
            CodeBlockView(.detectScreenSize)
        }
    }
}

#Preview {
    TopicDetectScreenSize()
}
