import SwiftUI

struct TopicContextMenu: View {
    var body: some View {
        VStack(spacing: 25) {
            Text("TopicContextMenu")
            
//            SimpleCodeBlockView(.image)
        }
    }
}

#Preview {
    TopicContextMenu()
}
